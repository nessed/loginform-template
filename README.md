# Login Form Template

Reusable front-end login form — markup, styling and validation states — kept as a
starting point so it does not get rebuilt from scratch each time. No auth backend
attached.

**Stack:** React · JavaScript

```sh
npm install
npm run dev
```
